{
  "name": "nestly-vercel-api",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Vercel serverless proxy for Nestly model recommendations (no secrets in repo)",
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "engines": {
    "node": "24.x"
  }
}
